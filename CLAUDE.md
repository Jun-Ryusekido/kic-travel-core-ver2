# CLAUDE.md

このファイルは、このリポジトリで作業する際に守るべきルールを記載する。

## テーブルのモバイル対応（標準パターン）

複数列のテーブル/グリッドを新規に追加する場合は、**必ず**共通コンポーネントの
`confirm-table` パターンを使い、768px未満でカード形式の縦積みに切り替えること
（運用者はスマホ中心。横スクロールが必要な画面を作らない）。

使い方（CSSは `index.html` の `@media(max-width:768px)` ブロックに定義済み）:

1. `<table class="confirm-table">` を付ける
2. 各 `<td>` に `data-label="列名"` を付ける（カード表示時のラベルになる）
3. チェックボックス・削除ボタン等ラベル不要のtdは data-label を付けない
4. セクション見出し行（colspanの帯）は `<tr class="section-row">` を付ける
5. tbody内にソート用ヘッダ行を持つ場合は `<tr class="cost-sort-row">` で モバイル時非表示

例外パターン（既存画面で使用中。新規では原則confirm-tableを使う）:
- `bl-table` + nth-child非表示: ダッシュボード/予約一覧（重要列のみ残す方式）
- `pt-desktop-only`/`pt-mobile-only`: 取引先マスタ（専用カードUI）
- `bt-desktop-only`/`bt-mobile-only`: 新幹線手配（専用カードUI）
- 印刷用プレビュー（Invoice/手配書/精算書等）: A4レイアウト再現のためカード化しない。
  `.tbl-wrap`（overflow-x:auto）で囲み、ページ全体の横スクロールは発生させないこと

変更後は Chrome DevTools のiPhone SE幅(375px)で対象画面に横スクロールが
発生しないことを確認すること。

## 日時の表示ルール

時刻を含む日時(timestamptz型の列。例: `created_at`/`updated_at`/`changed_at`等、
ISO 8601形式で`T`と時刻を含む文字列)を画面に表示する場合は、**必ず**
`new Date(v).toLocaleString('ja-JP')`で日本時間に変換してから表示すること。
DBにはUTCで保存されているため、変換せず生の文字列を表示すると他の日時表示箇所
（一覧のタイムスタンプ等）と食い違って見える不具合になる（変更履歴モーダルの
`auditFormatValue`で実際に発生した不具合）。

一方、時刻を含まない日付のみの列（`YYYY-MM-DD`。ツアーの開始日・利用日等の
暦日そのものを表す）は、タイムゾーン変換すると日付がずれる可能性があるため
**変換せずそのまま表示する**こと。両者を区別せず一律に`toLocaleString`を
通してしまうと、日付のみのフィールドで意図しない日ずれが起きる。

## ミールバウチャーテンプレートの変更ルール

`index.html` の `_buildVoucherHtml` / `generateMealVoucher` （ミールバウチャーPDF生成ロジック）
にレイアウト・座標・フォントスタイルに関わる変更を加える場合は、**必ず**以下を実行し、
全項目が一致することを確認してから完了報告すること。

```bash
python scripts/verify_voucher_layout.py
```

このスクリプトは、現在のテンプレートから実際にテストデータでPDFを生成し、
確定サンプル（`public/meal_voucher_sample.pdf`）から抽出したベースライン
（`voucher_layout_baseline.json`）と座標(top/left、許容誤差0.6mm)・
フォントスタイル(font-weight)を自動比較し、加えてラベル文字に罫線が重なって
意図しない下線に見えてしまう不具合がないかもチェックする。

不一致が出た場合は、それが以下のいずれかであることを確認すること:

1. **本当のレイアウト回帰** → テンプレートを修正して再実行し、一致させる。
2. **意図的な仕様変更に伴う正当な差分**（例: 手書き記入方式化による数値欄の
   空欄化・幅の変化など）→ `scripts/extract_voucher_baseline.py` 内で
   `skip_left_check_reason` 等により理由を明記した上でベースラインを更新し、
   `python scripts/extract_voucher_baseline.py` を再実行して
   `voucher_layout_baseline.json` を再生成する。

ベースライン自体（サンプルの解析結果）を作り直す必要がある場合のみ、
以下を実行する（通常は再実行不要。サンプルPDFが差し替わった場合等に限る）:

```bash
python scripts/extract_voucher_baseline.py
```

### 前提

- Python 3 + `pdfplumber`（`pip install pdfplumber`）
- Node.js（`_buildVoucherHtml` のソースをそのまま実行するため）
- Google ChromeまたはEdge（headlessでPDF印刷するため。既定パスが見つからない場合は
  環境変数 `CHROME_PATH` で実行ファイルのパスを指定する）

## 削除確認ダイアログのルール

削除操作（`confirm()`を伴うもの）を新規実装する場合、**必ず**削除対象の具体的な内容
（項目名・金額・氏名・日付等、その画面で意味のある情報）をダイアログの文言に含める
こと。「削除しますか？」のみの単純な確認は禁止。実装例: `removeCostRow`（支払先・
金額・備考）、`deleteFacilityTodoItem`（施設名・REF#・期限日）参照。

## type="date"入力欄のonchangeハンドラに関するルール

`type="date"`の入力欄は、既に値が入っている状態で一部（年/月/日のいずれか）を
1桁打っただけでも、それだけで完全な日付として成立してしまう場合は**その時点で
即座に`change`イベントが発火する**（2桁目の入力を待たない）。このためonchange
ハンドラ内で所属テーブル/セクション全体を`innerHTML`で再構築（例: 一覧描画関数を
丸ごと呼び直す）すると、その瞬間に入力欄自身のDOMノードが作り直されてフォーカスが
失われ、続けて打った2桁目以降のキー入力がどこにも反映されずロストする（例:
「27」と打ったつもりが「02」になり、都度確定→再修正が必要になる不具合。仕入明細の
出金日欄で実際に発生。修正はupdateCostPayDateCell参照）。

`type="date"`欄のonchangeハンドラで、値変更後に表示更新が必要な場合は、
**所属テーブル/セクション全体を再描画せず**、影響を受ける要素だけを
`textContent`/`style`等でピンポイントに更新すること（実装例:
`updateCostPayDateCell`（仕入明細・出金日の枠線色更新）、`bdSalesPaymentAmountCalc`
（売上明細・入金額の未収セル更新）、`hotelCalc`（ホテル泊数・金額のin-place更新）
参照）。どうしても一覧全体の再描画が避けられない場合は、`_dateInputFocusedIn`
ガードを流用し、フォーカス中の`type="date"`欄を含むコンテナの再描画はスキップする
こと。

## 画面遷移・タブ切替時のデータ再取得に関するルール

新しい画面・タブ・モーダルにデータ取得処理を追加する場合、**必ず**画面遷移や
タブ切替のたびに無条件で全件再取得しないこと。デフォルトで5分程度のTTL
キャッシュを設け、`select('*')`ではなく実際に使う列のみを指定すること。

これはSupabaseのegress(データ転送量)が過大になり課金・制限に直結した問題で、
過去に十数画面・機能で個別に後追い対応する事態になっている（`loadGuideAdvanceList`
（ガイド仮払い一覧）、`renderRestaurantConflicts`（レストラン重複確認）、
`renderTourCalendar`（ツアー運行カレンダー）、`loadFacilityManagementPage`
（観光地予約管理）、`loadVendorEmailPage`（手配確定状況）等、多数の画面で
「5分TTLキャッシュを導入」という同じ内容の修正が繰り返された）。新規実装の
最初からキャッシュを組み込み、事後対応を増やさないこと。

## onclick属性への値の焼き付けに関するルール

ボタン等のonclick属性に、その時点の行データ(仕入先名・日付等)をJSON等で
シリアライズして埋め込まないこと。onchangeが配列(メモリ上の状態)の更新のみを
行い再描画を伴わない設計と組み合わさると、行を最後に描画した後にユーザーが
入力欄を編集しても、ボタン側には描画時点の古い値が焼き付いたままになり、
クリック時に古い内容で処理されてしまう（手配タブの「仕入明細へ追加」ボタンで
実際に発生した不具合。日付欄を編集した後にクリックしても古い日付のまま
仕入明細に追加されていた）。ボタンには配列名+index等の「参照」だけを渡し、
クリック時に対象配列から最新値を読み直す方式にすること（実装例:
`addArrRowToCostNow`参照）。この不具合はより一般的な「データの陳腐化
（スナップショット追従漏れ）」パターンの代表例であり、一般原則は後述の
「データの陳腐化（スナップショット追従漏れ）に関するルール」を参照。

## 書き込み系API呼び出しの自動リトライに関するルール

`api/table-crud.js`に新しいactionを追加する場合、そのactionが読み取り専用
(何度実行しても結果が変わらない)でない限り、`tableCrudApiCall`の自動リトライ
対象に加えないこと。fetch自体が失敗した際の自動リトライを書き込み系action
(insert/updateById/deleteById等)にも一律適用していたため、サーバー側で処理が
既に完了(副作用が確定)した後にレスポンスが届く前だけネットワークが切れた
ケースを区別できず、リトライにより二重登録・二重更新を招くおそれがあった
（クレカ明細OCRの保存で同一内容が複数回保存される不具合の調査で発覚）。
読み取り専用actionの一覧は`TABLE_CRUD_IDEMPOTENT_READ_ACTIONS`にまとめてあり、
新しいactionはデフォルトでリトライしない安全側の設計とすること。

## updated_at等のタイムスタンプに関するルール

新しいテーブルへのINSERT/UPDATEを実装する場合、`updated_at`等の更新日時は
**必ず**サーバー側でスタンプすること。クライアント側で`new Date().toISOString()`
等を個別にセットする方式は、たまたま全箇所で一貫していただけでサーバー側の
保証が無く、差分バックアップから漏れる等の問題につながる(`email_import_queue`と
`bookings`の両方で同じ構造のリスクが実際に発生した)。`api/table-crud.js`の
`TABLE_CONFIG`に`stampUpdatedAt: true`を設定し、クライアントの自己申告値を
使わない方式に統一すること。

## 非同期サジェスト・候補検索の実装ルール

入力ごとに非同期検索(SELECT等)を発行するサジェスト・候補表示機能を実装する
場合、呼び出しごとに連番(seq)を発行し、応答が返ってきた時点でその呼び出しが
最新かどうかを確認してから描画すること。後から入力した(条件が厳しい)検索より、
先に入力した(条件が緩い)検索の応答が遅れて返ってくる場合があり、無条件に描画
すると古い検索結果が後から追加されて候補リストに残り続ける不具合になる
（REF#からコピーの候補サジェストで実際に発生。`arrCopyRefSuggestSeq`参照）。
また、1文字ごとに毎回SELECTを発行せず、200ms程度のデバウンスも入れること。

## モーダル内ポップアップ/サジェストのz-indexに関するルール

`document.body`直下に追加するポップアップ・サジェストボックス(モーダルの
子要素ではなく兄弟要素として追加されるもの)のz-indexを設定する場合、対象の
モーダル自身のz-indexより確実に高い値にすること。モーダル側のz-indexだけを
引き上げてポップアップ側を据え置くと、データ自体はDOM上に正しく生成されて
いても、モーダルの下に隠れて見えなくなる不具合になる（REF#からコピーの
候補サジェストで実際に発生）。どちらか一方のz-indexを変更する際は、もう一方も
必ず見直すこと。

## SQL実行漏れの防止に関するルール

テーブルへのカラム追加・RLS/GRANT変更等、DB側のスキーマ変更が必要なコード
変更を行う場合、コード側の変更とSQLの実行は**必ず**セットで完了報告すること。
SQL実行が未完了の場合は完了報告に含めず、「SQL未実行」であることを明示的に
申告すること。

## 金銭データへのSQL操作に関するルール

`booking_sales`/`booking_costs`/`invoices`等、金銭に関わるテーブルへのUPDATE/DELETE文を
SQLとして提示する場合、WHERE句に使うid等の値は**必ず直前に実行・確認されたSELECT結果
からのみ引用**すること。会話内でユーザーから伝えられた値であっても、Claude自身が
Supabaseへ直接アクセスして検証したものでなければ「未検証の値」として明示し、実行前に
対象を一意に絞り込むSELECT文を先に提示して結果確認を挟むこと。記憶・推測・過去の会話
内容からIDを構成して提示することは禁止する（対象を誤ると、無関係な予約のデータを
破壊するリスクがあるため）。

この確認手順は金銭に関わるテーブルのSQLに限らず、**削除・一括更新を伴う操作全般**
（SQL実行に限らず、管理画面上のバルク操作・移行スクリプトの実行等を含む）に適用する。
**必ず**以下の4ステップを徹底すること: (1) 対象データをJSON等でバックアップとして
保存する、(2) 対象件数・内容をユーザーに提示し確認プロンプトを挟む、(3) 承認後に
実行する、(4) 実行後に再度SELECT等で対象データを取得し、意図した変更になっているか
検証する。上記のWHERE句id値の検証はこの手順のうち(1)(2)の前段にあたる。

## 完了報告時の実データ検証に関するルール

「実装しました」「保存できました」という報告は、コードを書いた/API呼び出しを
行ったという事実だけでは完了報告としないこと。**必ず**実データへの反映を
SQL(SELECT)で確認する、またはUI操作であれば実際にクリックイベント等を発火させて
動作を確認してから完了報告すること。自己申告だけの報告は、実際には保存されて
いなかった・意図通りに反映されていなかった場合に気づかず進んでしまうリスクが
ある。

## 設計前の実データ確認に関するルール

新機能の追加・既存機能の改修を設計する場合、**必ず**着手前に実データ（対象の
Excel/スプレッドシート、運用画面のスクリーンショット、実際の運用フロー等）を
確認し、想定・推測だけで設計を確定させないこと。実データを見ずに設計すると、
運用実態と合わない項目・レイアウトになり、後から手戻りが発生する。

## データの陳腐化（スナップショット追従漏れ）に関するルール

あるデータA(仕入先名・金額・日付等)を元に、別の場所Bに値を生成・保持した場合、
その後Aが編集されてもBが古い値のまま追従しない構造(スナップショットの陳腐化)に
注意すること。「onclick属性への値の焼き付けに関するルール」（前述）はこの
パターンの代表例だが、onclick以外にもキャッシュ変数・コピーして保持した集計値・
別テーブルへコピーした金額等、同様のリスクは繰り返し発生している。Bを持つ場合は
値そのものではなく「参照」（id・index等）を保持し、必要な時点でAから最新値を
読み直す設計を優先すること。

## 二重管理の禁止に関するルール

同じ情報（金額・日付・ステータス等）を複数のテーブル/変数/UI要素に「正」として
重複して持たないこと（二重管理）。二重管理は、片方だけ更新して他方が古いまま
残るバグを構造的に生み続ける。可能な限り単一のソースを持ち、他の場所ではそこから
都度参照・計算する設計にすること。

既存データを新しい保持方法・テーブルに移行した場合は、移行前の参照元(旧カラム・
旧変数等)がコード中に残っていないか、`grep`等で機械的に洗い出して確認すること。
「この情報は今後どこか1箇所からしか参照されない状態か」を移行完了の条件とする。

## RLSポリシー削除・REVOKE作業の手順に関するルール

RLSポリシーを削除する場合、**必ず**事前に`pg_tables.rowsecurity`を確認し、
削除後に許可ポリシーが0件になるケース(意図せず全面アクセス不可/全面アクセス可能
になる)がないかをシミュレーションしてから実行すること。

REVOKE(anon等の権限剥奪)は、コード移行→マージ→本番経路での動作確認→REVOKE実行
→再検証、という順序を**必ず**厳守し、REVOKEを先行させないこと。先にREVOKEすると、
移行後のコードが本番で正しく動作するか確認する前に既存の経路が壊れ、切り戻しが
困難になる。

## SQL直接修正後の画面確認に関するルール

Supabase上でSQLによりデータを直接修正した場合、**必ず**ブラウザのハードリロード
（キャッシュを無視した再読み込み）を行った上で画面を確認すること。通常のリロード
では、画面遷移・タブ切替時の5分TTLキャッシュ（前述）やブラウザキャッシュにより、
SQL変更前の古い表示が残る場合がある。
